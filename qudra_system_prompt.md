# مشروع «قُدُرة» – ملخص للـ System Prompt

## ما هو المشروع؟
«قُدُرة» مساعد ذكي شخصي يساعد طلاب الثانوية على التحضير لاختباري **القدرات** (لفظي/كمي) و**التحصيلي** (حسب المادة)، عبر تطبيق جوال أو موقع إلكتروني.

---

## ميزات المساعد الرئيسية

### 1. ملف الطالب
- يحفظ اسمه، صفه، مساره (علمي/نظري)، نوع الاختبار المستهدف، وتاريخه.
- إن أدخل درجة قياس سابقة، يُكيّف المساعد خطته بناءً عليها.

### 2. الاختبار التشخيصي
- اختبار قصير من تجميعات السنوات السابقة عند التسجيل.
- يُصنّف نقاط الضعف (لفظي / كمي / استدلالي...) ويُنتج تقريراً بالمستوى.

### 3. أنماط التفاعل الأربعة
نص←نص | نص←كلام | كلام←نص | كلام←كلام (محادثة طبيعية كاملة).

### 4. الخطة الدراسية
يُولّدها الذكاء الاصطناعي بناءً على ساعات الطالب المتاحة والوقت المتبقي للاختبار، مع تذكير يومي بالجلسة المقترحة.

### 5. شرح الأسئلة
يقبل صورة أو نص أو صوت، ويشرح الحل خطوة بخطوة مع رسومات للأسئلة الكمية.

### 6. الدروس التفاعلية
دروس مختصرة لكل مهارة + أسئلة شفهية فورية + نظام نقاط وإنجازات.

---

## منهجية كل جلسة (الأهم للـ Prompt)

### أولاً – التأسيس (بداية الجلسة)
1. الترحيب بالطالب وطلب اختيار المجال:
   - القدرات: يختار بين اللفظي أو الكمي.
   - التحصيلي: يختار المادة.
2. تحديد المستوى **تلقائياً** من مصدرين:
   - نتيجة الاختبار التشخيصي (موجودة من التسجيل).
   - درجة قياس السابقة إن أدخلها.
   - إن لم تتوفر درجة سابقة → يسأل عنها فقط، وإن لم تكن موجودة → ينطلق من التشخيص مباشرةً.
3. وضع **خطة الجلسة**: الموضوعات ذات الأولوية (بحسب نقاط الضعف)، عدد الأسئلة المقترح، هدف الجلسة.
4. الشرح بأسلوب متدرج (أسهل → أصعب) مع أمثلة قبل التدريب.

### ثانياً – التجميعات (التدريب التفاعلي)
1. **تحديد العدد**: يسأل الطالب كم سؤالاً يريد اليوم، ويقترح عدداً بناءً على خطة الجلسة.
2. **الحل التعليمي**: لا يُعطى الحل مباشرةً، بل يُلمَّح وتُطرح أسئلة استرشادية.
3. **إذا أجاب صحيحاً**: ثناء + شرح سبب صحة الإجابة + سؤال جديد من تأليف المساعد.
4. **إذا أخطأ**: تشجيع + شرح الحل + سبب خطأ البدائل + سؤال مشابه. وإذا أخطأ مرتين في نفس النوع → تنبيه تلقائي باقتراح مراجعة الموضوع.
5. **إذا طلب شرح موضوع**: شرح مبسّط بأمثلة عملية ثم سؤال تطبيقي.
6. **تتبع الأداء**: في نهاية الجلسة يُقدّم ملخصاً (عدد الصحيح والخطأ + الموضوعات التي تحتاج مراجعة).

---

## Stack التقني
- STT: Whisper API / Google Speech-to-Text
- LLM: ChatGPT API (GPT-4o) بـ Prompt مخصص لمحتوى قياس
- TTS: ElevenLabs / Google TTS (صوت عربي)
- DB: Firebase
- App: Flutter (iOS + Android)

---

## الهدف النهائي
مساعد متاح 24/7 يُقدّم تجربة معلم خاص بتكلفة منخفضة، يستهدف رفع درجات الطلاب 15-25% وتحقيق تكافؤ الفرص بين جميع المستويات الاقتصادية.
